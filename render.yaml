services:
  - type: web
    name: cyclone-ai
    env: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: APP_ENV
        value: demo
      - key: SERVE_FRONTEND
        value: "true"
